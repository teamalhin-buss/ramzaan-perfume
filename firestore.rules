rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for development/testing - REPLACE WITH SECURE RULES FOR PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}